os:
  - linux
  - osx

language: python

python:
  - 2.7
  # - 3.4

env:
  - CLANG=3.9

before_install:
  - wget -O - http://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -
  - sudo apt-add-repository "deb http://apt.llvm.org/xenial/ llvm-toolchain-xenial-3.9 main"
  - sudo apt-get update
  - sudo apt-cache search clang
  - sudo apt-get install clang-3.9 lldb-3.9


  # - wget http://releases.llvm.org/3.9.0/clang+llvm-3.9.0-x86_64-apple-darwin.tar.xz
  # 
  # - sudo apt-get install libstdc++6
  # - wget -N http://releases.llvm.org/$CLANG.0/clang+llvm-$CLANG.0-x86_64-linux-gnu-ubuntu-14.04.tar.xz --directory-prefix=/tmp
  # - tar xpf /tmp/clang+llvm-$CLANG.0-x86_64-linux-gnu-ubuntu-14.04.tar.xz -C /tmp
  # - export PATH=/tmp/clang+llvm-$CLANG.0-x86_64-linux-gnu-ubuntu-14.04/bin:$PATH
  # - |
  #     if [[ -n "$PYENV_VERSION" ]]; then
  #       wget https://github.com/praekeltfoundation/travis-pyenv/releases/download/0.3.0/setup-pyenv.sh
  #       source setup-pyenv.sh
  #     fi

script:
  - clang-$CLANG --version
  - python --version
  - bash run-tests.sh
